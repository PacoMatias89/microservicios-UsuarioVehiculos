server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/PacoMatias89/microservicios-UsuarioVehiculos
          search-paths: config-data
  application:
    name: config-servicio        
